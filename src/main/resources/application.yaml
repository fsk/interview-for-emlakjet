spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/invoice_system
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddlAuto: update
    showSql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    databasePlatform: org.hibernate.dialect.PostgreSQLDialect

emlakjet:
  amount:
    value: 200